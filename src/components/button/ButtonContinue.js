import { StyleSheet, Text, View } from "react-native";
import React, { memo } from "react";

const ButtonContinue = () => {
  return (
    <View>
      <Text>ButtonContinue</Text>
    </View>
  );
};

export default memo(ButtonContinue);

const styles = StyleSheet.create({});
